
pub mod point {

    #[derive(Debug)]
    pub struct Point {
        pub x: f32,
        pub y: f32
    }

    #[derive(Debug)]
    pub struct PolarPoint {
        pub r: f32,
        pub t: f32
    }
}